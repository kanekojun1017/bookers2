
  <h2>User info</h2>
    <%= image_tag user.get_profile_image(100, 100) %>
      <table class="table">
        <tbody>
          <tr>
            <th>name</th>
              <th><%= user.name %></th>
          </tr>
          <tr>
            <th>introdaction</th>
            <th><%= user.introduction %></th>
          </tr>
        </tbody>
      </table>
      <div class="row">
        <a href="/users/<%= user.id %>/edit", class="btn btn-outline-secondary btn-block"></ahref>
          <i class="fa-solid fa-user-gear"></i>
        </a>
      </div>
